# VCS
.git
.gitignore
.gitattributes

# Python
__pycache__
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info
.eggs
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov
.tox

# Virtual environments
env
venv
.venv
ENV

# IDE / editor
.idea
.vscode
.history
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local secrets / env (keep these out of the image)
.env
.env.*
!.env.docker.example

# Build artifacts (generated inside the image)
staticfiles
media

# Docker / compose (no need to copy into the image)
Dockerfile
docker-compose*.yml
.dockerignore

# Project-local agent / docs
.claude
CLAUDE.md
docs

# Logs and temp
*.log
tmp
temp
*.tmp

# Node (if any frontend tooling appears later)
node_modules
